<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>

<body>
  <div class="container">
  

    <% if (locals.book) { %>
      <form id="editForm" method="post" action="/posts/<%=book.id%>">
        <input type="text" name="title" value="<%=book.title %>">
        <textarea name="content" rows="10"><%=book.content %></textarea>
        <input type="text" name="author" value="<%=book.author %>">
        <input type="date" name="date" value="<%=book.date %>">
        <input type="text" name="isbn" value="<%=book.isbn %>">
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
      <% } else {%>
        <form id="newPostForm" method="post" action="/posts">
          <input type="text" name="title" placeholder="Title" required>
          <textarea name="content" placeholder="Content" required rows="10"></textarea>
          <input type="text" name="author" placeholder="Author" required>
          <input type="date" name="date" placeholder="Date" required>
          <input type="text" name="isbn" placeholder="isbn" required>
          <button class="full-width" type="submit">
            <%= submit %>
          </button>
        </form>
        <% } %>

  </div>
</body>

</html>
